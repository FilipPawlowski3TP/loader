<Window x:Class="SecureLoader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:SecureLoader.ViewModels"
        mc:Ignorable="d"
        Title="SecureLoader" Height="500" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <Border Background="{StaticResource BgBrush}" CornerRadius="12" BorderBrush="#2D2D35" BorderThickness="1">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0">
                <TextBlock Text="SECURE LOADER" Style="{StaticResource MainTitle}"/>
                <Border Height="1" Background="#2D2D35" Margin="0,0,0,20"/>
            </StackPanel>

            <!-- Content -->
            <StackPanel Grid.Row="1">
                <TextBlock Text="LICENSE KEY" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="HARDWARE ID (HWID)" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Hwid, Mode=OneWay}" IsReadOnly="True" Background="#1A1A1F" Foreground="#71717A"/>

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="STATUS" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding Status}" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="EXPIRATION" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding ExpirationDate}" Foreground="{StaticResource TextBrush}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="2">
                <Button Content="LOGIN" Command="{Binding LoginCommand}" Margin="0,0,0,10"/>
                <Button Content="LAUNCH APPLICATION" Command="{Binding LaunchCommand}" 
                        IsEnabled="{Binding IsLaunchEnabled}" Style="{StaticResource AccentButton}"/>
                
                <Button Content="EXIT" Click="Exit_Click" Background="Transparent" Foreground="{StaticResource MutedTextBrush}" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
